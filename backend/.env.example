# Comma-separated list of allowed frontend origins for CORS.
# Use "*" to allow all origins (fine for local dev, not recommended for production).
# Example for production: https://jobfit-ai.vercel.app
ALLOWED_ORIGINS=*